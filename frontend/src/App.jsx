import { useState } from "react";
import { Helmet } from "react-helmet";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className="main">
        <h1>What's uppppp</h1>
    </div>
  );
}

export default App;
